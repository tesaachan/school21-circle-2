#include "../include/push_swap.h"

void	stacks_init_stack_b(stacks_t *stacks)
{
    stacks->b = NULL;
    stacks->b_length = 0;
    if (stacks->a_length == 5)
    {
        
    }
    else
    {
        
    }
}
